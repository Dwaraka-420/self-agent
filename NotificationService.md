# NotificationService.ts

## Purpose
Manages user notifications within the application.

## Features
- **Fetch Notifications**: Retrieves user notifications from the backend.
- **Mark Notifications as Read**: Updates status to "read."

## Example
```typescript
const unread = await notificationService.getUnreadCount();
```
